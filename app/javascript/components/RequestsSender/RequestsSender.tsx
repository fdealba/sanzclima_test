import * as React from 'react';
import classes from './RequestsSender.module.scss';

export const RequestsSender = () => {
  return (
  <div className={classes.RequestsSender}>
    <h3>Im the Request Sender:</h3>
  </div>);
}
